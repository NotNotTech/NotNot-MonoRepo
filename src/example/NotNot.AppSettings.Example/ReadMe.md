# Example project

A working example showing how to use the NotNot.AppSettings nuget package

This sample now keeps `appsettings.json` host-only and moves typed sample data into `appsettings.Sample.json`.
For the full normalization recipe, see [`docs/appsettings-normalization.md`](../../../../../docs/appsettings-normalization.md).
